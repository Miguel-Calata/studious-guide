# Base URL del backend expuesta al navegador.
# En dev usa el proxy de Vite (/api/v1). En prod apunta al dominio real.
VITE_API_BASE_URL=/api/v1

# Origen del backend para peticiones CORS (no se usa en el frontend directamente,
# pero se documenta aquí para referencia de despliegue).
# BACKEND_ORIGIN=https://api.astreo.space
